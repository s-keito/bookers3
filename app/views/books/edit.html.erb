<h1>Editing Book</h1>

<form>
<%= render 'form', book: @book, model: @book, url: book_path(@book.id), method: :patch %>
</form>

<%= link_to 'Show', book_path(@book.id) %> |
<%= link_to 'Back', books_path %>